# Copy to client/.env. CRA only exposes vars prefixed with REACT_APP_.
# Must point at the same backend the server is actually served from
# (used for both REST calls and the Socket.io connection).
REACT_APP_SERVER_ACCESS_BASE_URL=http://localhost:4000
